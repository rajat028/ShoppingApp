<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorErrorBackground">

    <TextView
        android:id="@+id/tvError"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-thin"
        android:gravity="center"
        android:includeFontPadding="false"
        android:paddingLeft="@dimen/error_message_padding_horizontal"
        android:paddingRight="@dimen/error_message_padding_horizontal"
        android:text="@string/error_message"
        android:textColor="@android:color/white"
        android:textSize="@dimen/error_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnRetry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/retry_button_margin_top"
        android:background="@color/colorRetryButton"
        android:paddingLeft="@dimen/retry_button_padding_horizontal"
        android:paddingRight="@dimen/retry_button_padding_horizontal"
        android:text="@string/retry_button_text"
        android:textColor="@android:color/white"
        android:textSize="@dimen/retry_button_text_size"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tvError" />
</android.support.constraint.ConstraintLayout>
